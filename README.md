Sets up a very minimal API for todo list tasks, integrated with telemetrics, mainly to demonstrate observability.

## To Run:

Navigate to esep-observability/ directory in terminal; run `node ./index.js`
